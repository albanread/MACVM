//! MACVM entry point (placeholder).
//!
//! The VM is at the scaffold stage; this just proves the crate builds and links.

fn main() {
    println!("MACVM — Self/Strongtalk-lineage research VM (arm64). Scaffold only.");
}
