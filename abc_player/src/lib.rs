//! NOTYETIMPLEMENTED.
//!
//! Reserved for an ABC-notation tune player + game-style SFX synthesizer.
//! A port was started and then paused: no Rust source for this exists
//! anywhere on this machine (checked directly, incl. every sister repo
//! under `~/claudeprojects` and a broader home-directory search) — the
//! true origin (`newaudio-core`/`newaudio-abc`) lives only at a Windows
//! path (`E:\NewAudio\NewAudio\`) that isn't checked out here. Two real,
//! Mac-working (non-Rust) references exist to port from instead:
//! `~/claudeprojects/MacModula2` (`library/sharedmod/Audio.mod`/`Abc.mod`,
//! a faithful Modula-2 port, already fully read) and `~/Framework/Audio`
//! (a C++/Objective-C++ engine — `ABCParser`/`SynthEngine`/`MidiEngine`/
//! `CoreAudioEngine`, part of the "SuperTerminal v2" project). Which one
//! (if either) to port from next is not yet decided — do not resume this
//! port without checking in first.
